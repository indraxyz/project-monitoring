import Layout from "../../components/Layout";

const Approvals = () => {
  // state

  // approval progress

  // filter progress (months, search by job number)

  return (
    <Layout>
      <span>Approval Page</span>

      {/* data table */}

      {/* modal: detail project, detail progress */}
    </Layout>
  );
};

export default Approvals;
